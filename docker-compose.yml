services:
  gateway:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: layer-gateway-api-v1
    env_file:
      - .env
    environment:
      - PORT=8000
    ports:
      - "8000:8000"
    restart: unless-stopped
